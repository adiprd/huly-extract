"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var utils_exports = {};
__export(utils_exports, {
  getWorkspaceToken: () => getWorkspaceToken
});
module.exports = __toCommonJS(utils_exports);
var import_account_client = require("@hcengineering/account-client");
var import_config = require("./config");
async function getWorkspaceToken(url, options, config) {
  config ??= await (0, import_config.loadServerConfig)(url);
  let token;
  if ("token" in options) {
    token = options.token;
  } else {
    const { email, password } = options;
    const loginInfo = await (0, import_account_client.getClient)(config.ACCOUNTS_URL).login(email, password);
    token = loginInfo.token;
  }
  if (token === void 0) {
    throw new Error("Login failed");
  }
  const ws = await (0, import_account_client.getClient)(config.ACCOUNTS_URL, token).selectWorkspace(options.workspace);
  if (ws === void 0) {
    throw new Error("Workspace not found");
  }
  return { endpoint: ws.endpoint, token: ws.token, workspaceId: ws.workspace, info: ws };
}
__name(getWorkspaceToken, "getWorkspaceToken");
//# sourceMappingURL=utils.js.map
