import { Node } from '@tiptap/core';
import type { Blob, Ref } from '@hcengineering/core';
declare module '@tiptap/core' {
    interface Commands<ReturnType> {
        emoji: {
            insertEmoji: (emoji: string, kind: 'unicode' | 'image', image?: Ref<Blob>) => ReturnType;
        };
    }
}
export interface EmojiNodeOptions {
    getBlobRef: (fileId: Ref<Blob>, filename?: string, size?: number) => Promise<{
        src: string;
        srcset: string;
    }>;
}
export declare const EmojiNode: Node<EmojiNodeOptions, any>;
//# sourceMappingURL=emoji.d.ts.map