"use strict";
var import_operator = require("../operator");
describe("operator edge cases and potential bugs", () => {
  describe("$push operator edge cases", () => {
    it("BUG: $push with $each on null field should handle gracefully", () => {
      const doc = { _id: "1", _class: "test", arr: null };
      const operator = (0, import_operator._getOperator)("$push");
      operator(doc, { arr: { $each: [1, 2, 3] } });
      expect(doc.arr).toBe(null);
    });
    it("BUG: $push with $each on non-array field should handle gracefully", () => {
      const doc = { _id: "1", _class: "test", arr: "string" };
      const operator = (0, import_operator._getOperator)("$push");
      operator(doc, { arr: { $each: [1, 2, 3] } });
      expect(doc.arr).toBe("string");
    });
    it("$push without $each on null field works correctly", () => {
      const doc = { _id: "1", _class: "test", arr: null };
      const operator = (0, import_operator._getOperator)("$push");
      operator(doc, { arr: "value" });
      expect(doc.arr).toEqual(["value"]);
    });
    it("$push without $each on non-array field reports error and fixes", () => {
      const doc = { _id: "1", _class: "test", arr: "string" };
      const operator = (0, import_operator._getOperator)("$push");
      operator(doc, { arr: "value" });
      expect(doc.arr).toEqual(["value"]);
    });
  });
  describe("$pull operator edge cases", () => {
    it("should initialize undefined field as empty array", () => {
      const doc = { _id: "1", _class: "test" };
      const operator = (0, import_operator._getOperator)("$pull");
      operator(doc, { arr: "value" });
      expect(doc.arr).toEqual([]);
    });
    it("should handle $in with empty array", () => {
      const doc = { _id: "1", _class: "test", arr: [1, 2, 3] };
      const operator = (0, import_operator._getOperator)("$pull");
      operator(doc, { arr: { $in: [] } });
      expect(doc.arr).toEqual([1, 2, 3]);
    });
    it("should handle complex objects with partial matches", () => {
      const doc = {
        _id: "1",
        _class: "test",
        arr: [
          { a: 1, b: 2, c: 3 },
          { a: 1, b: 2, c: 4 },
          { a: 2, b: 2, c: 3 }
        ]
      };
      const operator = (0, import_operator._getOperator)("$pull");
      operator(doc, { arr: { a: 1, b: 2 } });
      expect(doc.arr).toEqual([{ a: 2, b: 2, c: 3 }]);
    });
  });
  describe("$update operator edge cases", () => {
    it("should handle query with multiple conditions", () => {
      const doc = {
        _id: "1",
        _class: "test",
        arr: [
          { name: "a", status: "active", value: 1 },
          { name: "a", status: "inactive", value: 2 },
          { name: "b", status: "active", value: 3 }
        ]
      };
      const operator = (0, import_operator._getOperator)("$update");
      operator(doc, {
        arr: {
          $query: { name: "a", status: "active" },
          $update: { value: 100 }
        }
      });
      expect(doc.arr).toEqual([
        { name: "a", status: "active", value: 100 },
        { name: "a", status: "inactive", value: 2 },
        { name: "b", status: "active", value: 3 }
      ]);
    });
    it("should add new fields in update", () => {
      const doc = {
        _id: "1",
        _class: "test",
        arr: [{ name: "a" }]
      };
      const operator = (0, import_operator._getOperator)("$update");
      operator(doc, {
        arr: {
          $query: { name: "a" },
          $update: { newField: "newValue" }
        }
      });
      expect(doc.arr).toEqual([{ name: "a", newField: "newValue" }]);
    });
  });
  describe("$inc operator edge cases", () => {
    it("should handle floating point numbers", () => {
      const doc = { _id: "1", _class: "test", value: 1.5 };
      const operator = (0, import_operator._getOperator)("$inc");
      operator(doc, { value: 2.3 });
      expect(doc.value).toBeCloseTo(3.8);
    });
    it("should handle very large numbers", () => {
      const doc = { _id: "1", _class: "test", value: Number.MAX_SAFE_INTEGER - 1 };
      const operator = (0, import_operator._getOperator)("$inc");
      operator(doc, { value: 1 });
      expect(doc.value).toBe(Number.MAX_SAFE_INTEGER);
    });
    it("should handle negative numbers to zero", () => {
      const doc = { _id: "1", _class: "test", count: 5 };
      const operator = (0, import_operator._getOperator)("$inc");
      operator(doc, { count: -5 });
      expect(doc.count).toBe(0);
    });
  });
  describe("$rename operator edge cases", () => {
    it("BUG: should handle renaming to the same name causes deletion", () => {
      const doc = { _id: "1", _class: "test", field: "value" };
      const operator = (0, import_operator._getOperator)("$rename");
      operator(doc, { field: "field" });
      expect(doc.field).toBeUndefined();
    });
    it("BUG: chain renaming has race condition", () => {
      const doc = { _id: "1", _class: "test", a: 1, b: 2, c: 3 };
      const operator = (0, import_operator._getOperator)("$rename");
      operator(doc, { a: "b", b: "c" });
      expect(doc.a).toBeUndefined();
      expect(doc.b).toBeUndefined();
      expect(doc.c).toBe(1);
    });
  });
  describe("$unset operator edge cases", () => {
    it("should handle unsetting nested properties (only top level)", () => {
      const doc = {
        _id: "1",
        _class: "test",
        obj: { nested: "value", other: "data" }
      };
      const operator = (0, import_operator._getOperator)("$unset");
      operator(doc, { obj: "" });
      expect(doc.obj).toBeUndefined();
    });
    it("should handle unsetting array elements", () => {
      const doc = { _id: "1", _class: "test", arr: [1, 2, 3] };
      const operator = (0, import_operator._getOperator)("$unset");
      operator(doc, { arr: "" });
      expect(doc.arr).toBeUndefined();
    });
  });
});
//# sourceMappingURL=operator-bugs.test.js.map
