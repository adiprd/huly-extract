"use strict";
var import_collaboration = require("../collaboration");
describe("collaboration", () => {
  describe("makeCollabId", () => {
    it("should create collaborative doc id from parameters", () => {
      const objectClass = "class:core.Doc";
      const objectId = "doc123";
      const objectAttr = "content";
      const result = (0, import_collaboration.makeCollabId)(objectClass, objectId, objectAttr);
      expect(result).toEqual({
        objectClass,
        objectId,
        objectAttr
      });
    });
    it("should handle different attribute types", () => {
      const objectClass = "class:task.Task";
      const objectId = "task456";
      const result1 = (0, import_collaboration.makeCollabId)(objectClass, objectId, "description");
      const result2 = (0, import_collaboration.makeCollabId)(objectClass, objectId, "comments");
      expect(result1.objectAttr).toBe("description");
      expect(result2.objectAttr).toBe("comments");
    });
    it("should create unique ids for same object but different attributes", () => {
      const objectClass = "class:core.Doc";
      const objectId = "doc123";
      const result1 = (0, import_collaboration.makeCollabId)(objectClass, objectId, "attr1");
      const result2 = (0, import_collaboration.makeCollabId)(objectClass, objectId, "attr2");
      expect(result1.objectAttr).not.toBe(result2.objectAttr);
    });
  });
  describe("makeDocCollabId", () => {
    it("should create collaborative doc id from document", () => {
      const doc = {
        _id: "doc789",
        _class: "class:core.Doc",
        space: "space1",
        modifiedOn: 12345,
        modifiedBy: "user1"
      };
      const result = (0, import_collaboration.makeDocCollabId)(doc, "content");
      expect(result).toEqual({
        objectClass: doc._class,
        objectId: doc._id,
        objectAttr: "content"
      });
    });
    it("should work with typed attribute keys", () => {
      const doc = {
        _id: "doc999",
        _class: "class:test.TestDoc",
        space: "space1",
        modifiedOn: 12345,
        modifiedBy: "user1",
        title: "Test",
        description: "Description"
      };
      const result = (0, import_collaboration.makeDocCollabId)(doc, "description");
      expect(result.objectAttr).toBe("description");
    });
    it("should handle documents with complex ids", () => {
      const doc = {
        _id: "space:task:project-1:task-123",
        _class: "class:task.Task",
        space: "space1",
        modifiedOn: 12345,
        modifiedBy: "user1"
      };
      const result = (0, import_collaboration.makeDocCollabId)(doc, "content");
      expect(result.objectId).toBe("space:task:project-1:task-123");
    });
  });
  describe("makeCollabYdocId", () => {
    it("should create ydoc id from collaborative doc", () => {
      const collabDoc = {
        objectClass: "class:core.Doc",
        objectId: "doc123",
        objectAttr: "content"
      };
      const result = (0, import_collaboration.makeCollabYdocId)(collabDoc);
      expect(result).toBe("doc123%content");
    });
    it("should use % as separator", () => {
      const collabDoc = {
        objectClass: "class:core.Doc",
        objectId: "myDoc",
        objectAttr: "myAttr"
      };
      const result = (0, import_collaboration.makeCollabYdocId)(collabDoc);
      expect(result).toContain("%");
      expect(result.split("%")).toHaveLength(2);
    });
    it("should create consistent ids", () => {
      const collabDoc = {
        objectClass: "class:core.Doc",
        objectId: "doc456",
        objectAttr: "field"
      };
      const result1 = (0, import_collaboration.makeCollabYdocId)(collabDoc);
      const result2 = (0, import_collaboration.makeCollabYdocId)(collabDoc);
      expect(result1).toBe(result2);
    });
    it("should create different ids for different attributes", () => {
      const collabDoc1 = {
        objectClass: "class:core.Doc",
        objectId: "doc789",
        objectAttr: "content"
      };
      const collabDoc2 = {
        objectClass: "class:core.Doc",
        objectId: "doc789",
        objectAttr: "description"
      };
      const result1 = (0, import_collaboration.makeCollabYdocId)(collabDoc1);
      const result2 = (0, import_collaboration.makeCollabYdocId)(collabDoc2);
      expect(result1).not.toBe(result2);
    });
    it("should handle special characters in ids", () => {
      const collabDoc = {
        objectClass: "class:core.Doc",
        objectId: "doc:with:colons",
        objectAttr: "attr-with-dash"
      };
      const result = (0, import_collaboration.makeCollabYdocId)(collabDoc);
      expect(result).toBe("doc:with:colons%attr-with-dash");
    });
  });
  describe("makeCollabJsonId", () => {
    beforeEach(() => {
      jest.useFakeTimers();
    });
    afterEach(() => {
      jest.useRealTimers();
    });
    it("should create json id with timestamp", () => {
      const mockDate = /* @__PURE__ */ new Date("2024-01-01T00:00:00.000Z");
      jest.setSystemTime(mockDate);
      const collabDoc = {
        objectClass: "class:core.Doc",
        objectId: "doc123",
        objectAttr: "content"
      };
      const result = (0, import_collaboration.makeCollabJsonId)(collabDoc);
      expect(result).toBe(`doc123-content-${mockDate.getTime()}`);
    });
    it("should use - as separator", () => {
      const collabDoc = {
        objectClass: "class:core.Doc",
        objectId: "myDoc",
        objectAttr: "myAttr"
      };
      const result = (0, import_collaboration.makeCollabJsonId)(collabDoc);
      const parts = result.split("-");
      expect(parts.length).toBeGreaterThanOrEqual(3);
      expect(parts[0]).toBe("myDoc");
      expect(parts[1]).toBe("myAttr");
    });
    it("should create different ids when called at different times", () => {
      const collabDoc = {
        objectClass: "class:core.Doc",
        objectId: "doc456",
        objectAttr: "field"
      };
      jest.setSystemTime(/* @__PURE__ */ new Date("2024-01-01T00:00:00.000Z"));
      const result1 = (0, import_collaboration.makeCollabJsonId)(collabDoc);
      jest.setSystemTime(/* @__PURE__ */ new Date("2024-01-01T00:00:01.000Z"));
      const result2 = (0, import_collaboration.makeCollabJsonId)(collabDoc);
      expect(result1).not.toBe(result2);
    });
    it("should include all three components", () => {
      const mockDate = /* @__PURE__ */ new Date("2024-06-15T12:30:45.000Z");
      jest.setSystemTime(mockDate);
      const collabDoc = {
        objectClass: "class:core.Doc",
        objectId: "testDoc",
        objectAttr: "testAttr"
      };
      const result = (0, import_collaboration.makeCollabJsonId)(collabDoc);
      expect(result).toContain("testDoc");
      expect(result).toContain("testAttr");
      expect(result).toContain(mockDate.getTime().toString());
    });
    it("should handle objects with ids containing dashes", () => {
      const mockDate = /* @__PURE__ */ new Date("2024-01-01T00:00:00.000Z");
      jest.setSystemTime(mockDate);
      const collabDoc = {
        objectClass: "class:core.Doc",
        objectId: "doc-with-dashes",
        objectAttr: "attr-with-dashes"
      };
      const result = (0, import_collaboration.makeCollabJsonId)(collabDoc);
      expect(result).toBe(`doc-with-dashes-attr-with-dashes-${mockDate.getTime()}`);
    });
  });
  describe("integration tests", () => {
    it("should create different types of ids from same collaborative doc", () => {
      const collabDoc = {
        objectClass: "class:core.Doc",
        objectId: "doc123",
        objectAttr: "content"
      };
      const ydocId = (0, import_collaboration.makeCollabYdocId)(collabDoc);
      const jsonId = (0, import_collaboration.makeCollabJsonId)(collabDoc);
      expect(ydocId).not.toBe(jsonId);
      expect(ydocId).toContain(collabDoc.objectId);
      expect(jsonId).toContain(collabDoc.objectId);
    });
    it("should work with makeDocCollabId and other functions", () => {
      const doc = {
        _id: "doc999",
        _class: "class:core.Doc",
        space: "space1",
        modifiedOn: 12345,
        modifiedBy: "user1"
      };
      const collabDoc = (0, import_collaboration.makeDocCollabId)(doc, "content");
      const ydocId = (0, import_collaboration.makeCollabYdocId)(collabDoc);
      const jsonId = (0, import_collaboration.makeCollabJsonId)(collabDoc);
      expect(ydocId).toContain("doc999");
      expect(jsonId).toContain("doc999");
    });
  });
});
//# sourceMappingURL=collaboration.test.js.map
