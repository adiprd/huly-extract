"use strict";
var import_measurements = require("@hcengineering/measurements");
describe("context tests", () => {
  it("check withLog proper catch", async () => {
    const ctx = new import_measurements.MeasureMetricsContext("test", {});
    try {
      await ctx.with(
        "failed op",
        {},
        async () => {
          throw new Error("failed");
        },
        void 0,
        { log: true }
      );
      expect(true).toBe(false);
    } catch (err) {
      expect(err.message).toBe("failed");
    }
  });
});
//# sourceMappingURL=contexts.test.js.map
