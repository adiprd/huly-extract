"use strict";
var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var import_clone = require("../clone");
describe("clone", () => {
  it("should handle primitive types", () => {
    expect((0, import_clone.clone)(void 0)).toBeUndefined();
    expect((0, import_clone.clone)(null)).toBeNull();
    expect((0, import_clone.clone)(123)).toBe(123);
    expect((0, import_clone.clone)("test")).toBe("test");
    expect((0, import_clone.clone)(true)).toBe(true);
  });
  it("should clone Date objects", () => {
    const date = /* @__PURE__ */ new Date();
    const cloned = (0, import_clone.clone)(date);
    expect(cloned).toBeInstanceOf(Date);
    expect(cloned.getTime()).toBe(date.getTime());
  });
  it("should clone Arrays", () => {
    const arr = [1, "test", { a: 1 }, /* @__PURE__ */ new Date()];
    const cloned = (0, import_clone.clone)(arr);
    expect(cloned).toEqual(arr);
    expect(cloned).not.toBe(arr);
    expect(cloned[2]).not.toBe(arr[2]);
    expect(cloned[3]).not.toBe(arr[3]);
  });
  it("should clone Objects", () => {
    const obj = {
      num: 123,
      str: "test",
      date: /* @__PURE__ */ new Date(),
      nested: { a: 1 },
      arr: [1, 2, 3]
    };
    const cloned = (0, import_clone.clone)(obj);
    expect(cloned).toEqual(obj);
    expect(cloned).not.toBe(obj);
    expect(cloned.nested).not.toBe(obj.nested);
    expect(cloned.arr).not.toBe(obj.arr);
    expect(cloned.date).not.toBe(obj.date);
  });
  it("should respect depth parameter", () => {
    const deep = { a: { b: { c: { d: 1 } } } };
    expect((0, import_clone.clone)(deep, void 0, void 0, 0)).toBe(deep);
    expect((0, import_clone.clone)(deep, void 0, void 0, 1)).toEqual({ a: { b: { c: { d: 1 } } } });
    expect((0, import_clone.clone)(deep, void 0, void 0, 2)).toEqual({ a: { b: { c: { d: 1 } } } });
    expect((0, import_clone.clone)(deep, void 0, void 0, 3)).toEqual({ a: { b: { c: { d: 1 } } } });
  });
  it("should handle functions", () => {
    const fn = /* @__PURE__ */ __name(() => {
    }, "fn");
    expect((0, import_clone.clone)(fn)).toBe(fn);
  });
});
describe("getTypeOf", () => {
  it("should detect types correctly", () => {
    expect((0, import_clone.getTypeOf)(null)).toBe("null");
    expect((0, import_clone.getTypeOf)([])).toBe("Array");
    expect((0, import_clone.getTypeOf)({})).toBe("Object");
    expect((0, import_clone.getTypeOf)(/* @__PURE__ */ new Date())).toBe("Date");
    expect((0, import_clone.getTypeOf)(/test/)).toBe("RegExp");
    expect((0, import_clone.getTypeOf)(123)).toBe("number");
    expect((0, import_clone.getTypeOf)("test")).toBe("string");
  });
});
//# sourceMappingURL=clone.test.js.map
