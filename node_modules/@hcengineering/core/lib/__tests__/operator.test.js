"use strict";
var import_operator = require("../operator");
describe("operator", () => {
  describe("isOperator", () => {
    it("should return true for operator objects", () => {
      expect((0, import_operator.isOperator)({ $push: { arr: 1 } })).toBe(true);
      expect((0, import_operator.isOperator)({ $pull: { arr: 1 } })).toBe(true);
      expect((0, import_operator.isOperator)({ $inc: { count: 1 } })).toBe(true);
      expect((0, import_operator.isOperator)({ $unset: { field: "" } })).toBe(true);
      expect((0, import_operator.isOperator)({ $rename: { oldField: "newField" } })).toBe(true);
      expect((0, import_operator.isOperator)({ $update: { arr: {} } })).toBe(true);
    });
    it("should return true for multiple operators", () => {
      expect((0, import_operator.isOperator)({ $push: { arr: 1 }, $inc: { count: 1 } })).toBe(true);
    });
    it("should return false for non-operator objects", () => {
      expect((0, import_operator.isOperator)({ field: "value" })).toBe(false);
      expect((0, import_operator.isOperator)({ _id: "123" })).toBe(false);
      expect((0, import_operator.isOperator)({ name: "test", value: 123 })).toBe(false);
    });
    it("should return false for empty objects", () => {
      expect((0, import_operator.isOperator)({})).toBe(false);
    });
    it("should return false for null or non-objects", () => {
      expect((0, import_operator.isOperator)(null)).toBe(false);
      expect((0, import_operator.isOperator)(void 0)).toBe(false);
      expect((0, import_operator.isOperator)("string")).toBe(false);
      expect((0, import_operator.isOperator)(123)).toBe(false);
      expect((0, import_operator.isOperator)([])).toBe(false);
    });
    it("should return false for mixed operator and non-operator keys", () => {
      expect((0, import_operator.isOperator)({ $push: { arr: 1 }, field: "value" })).toBe(false);
    });
  });
  describe("$push operator", () => {
    it("should push a single value to an array", () => {
      const doc = { _id: "1", _class: "test" };
      const operator = (0, import_operator._getOperator)("$push");
      operator(doc, { arr: "value" });
      expect(doc.arr).toEqual(["value"]);
    });
    it("should push multiple values with $each", () => {
      const doc = { _id: "1", _class: "test", arr: [1, 2] };
      const operator = (0, import_operator._getOperator)("$push");
      operator(doc, { arr: { $each: [3, 4, 5] } });
      expect(doc.arr).toEqual([3, 4, 5, 1, 2]);
    });
    it("should push with $position", () => {
      const doc = { _id: "1", _class: "test", arr: [1, 2, 3] };
      const operator = (0, import_operator._getOperator)("$push");
      operator(doc, { arr: { $each: [10, 20], $position: 1 } });
      expect(doc.arr).toEqual([1, 10, 20, 2, 3]);
    });
    it("should push object to array", () => {
      const doc = { _id: "1", _class: "test", arr: [] };
      const operator = (0, import_operator._getOperator)("$push");
      const obj = { name: "test", value: 123 };
      operator(doc, { arr: obj });
      expect(doc.arr).toEqual([obj]);
    });
    it("should initialize undefined field as array", () => {
      const doc = { _id: "1", _class: "test" };
      const operator = (0, import_operator._getOperator)("$push");
      operator(doc, { newArr: "value" });
      expect(doc.newArr).toEqual(["value"]);
    });
    it("should handle null array field", () => {
      const doc = { _id: "1", _class: "test", arr: null };
      const operator = (0, import_operator._getOperator)("$push");
      operator(doc, { arr: "value" });
      expect(doc.arr).toEqual(["value"]);
    });
    it("should handle pushing to non-array field", () => {
      const doc = { _id: "1", _class: "test", field: "string" };
      const operator = (0, import_operator._getOperator)("$push");
      operator(doc, { field: "value" });
      expect(doc.field).toEqual(["value"]);
    });
  });
  describe("$pull operator", () => {
    it("should pull a single value from array", () => {
      const doc = { _id: "1", _class: "test", arr: [1, 2, 3, 2] };
      const operator = (0, import_operator._getOperator)("$pull");
      operator(doc, { arr: 2 });
      expect(doc.arr).toEqual([1, 3]);
    });
    it("should pull multiple values with $in", () => {
      const doc = { _id: "1", _class: "test", arr: [1, 2, 3, 4, 5] };
      const operator = (0, import_operator._getOperator)("$pull");
      operator(doc, { arr: { $in: [2, 4] } });
      expect(doc.arr).toEqual([1, 3, 5]);
    });
    it("should pull objects matching all fields", () => {
      const doc = {
        _id: "1",
        _class: "test",
        arr: [
          { name: "a", value: 1 },
          { name: "b", value: 2 },
          { name: "a", value: 3 }
        ]
      };
      const operator = (0, import_operator._getOperator)("$pull");
      operator(doc, { arr: { name: "a", value: 1 } });
      expect(doc.arr).toEqual([
        { name: "b", value: 2 },
        { name: "a", value: 3 }
      ]);
    });
    it("should handle undefined field", () => {
      const doc = { _id: "1", _class: "test" };
      const operator = (0, import_operator._getOperator)("$pull");
      operator(doc, { arr: "value" });
      expect(doc.arr).toEqual([]);
    });
    it("should handle null array", () => {
      const doc = { _id: "1", _class: "test", arr: null };
      const operator = (0, import_operator._getOperator)("$pull");
      operator(doc, { arr: "value" });
      expect(doc.arr).toEqual([]);
    });
    it("should not pull when object fields do not all match", () => {
      const doc = {
        _id: "1",
        _class: "test",
        arr: [
          { name: "a", value: 1 },
          { name: "b", value: 2 }
        ]
      };
      const operator = (0, import_operator._getOperator)("$pull");
      operator(doc, { arr: { name: "a", value: 999 } });
      expect(doc.arr).toEqual([
        { name: "a", value: 1 },
        { name: "b", value: 2 }
      ]);
    });
  });
  describe("$update operator", () => {
    it("should update matching array elements", () => {
      const doc = {
        _id: "1",
        _class: "test",
        arr: [
          { name: "a", value: 1 },
          { name: "b", value: 2 },
          { name: "a", value: 3 }
        ]
      };
      const operator = (0, import_operator._getOperator)("$update");
      operator(doc, {
        arr: {
          $query: { name: "a" },
          $update: { value: 100 }
        }
      });
      expect(doc.arr).toEqual([
        { name: "a", value: 100 },
        { name: "b", value: 2 },
        { name: "a", value: 100 }
      ]);
    });
    it("should handle empty array", () => {
      const doc = { _id: "1", _class: "test", arr: [] };
      const operator = (0, import_operator._getOperator)("$update");
      operator(doc, {
        arr: {
          $query: { name: "a" },
          $update: { value: 100 }
        }
      });
      expect(doc.arr).toEqual([]);
    });
    it("should handle undefined field", () => {
      const doc = { _id: "1", _class: "test" };
      const operator = (0, import_operator._getOperator)("$update");
      operator(doc, {
        arr: {
          $query: { name: "a" },
          $update: { value: 100 }
        }
      });
      expect(doc.arr).toEqual([]);
    });
    it("should update multiple fields", () => {
      const doc = {
        _id: "1",
        _class: "test",
        arr: [{ name: "a", value: 1, extra: "old" }]
      };
      const operator = (0, import_operator._getOperator)("$update");
      operator(doc, {
        arr: {
          $query: { name: "a" },
          $update: { value: 100, extra: "new" }
        }
      });
      expect(doc.arr).toEqual([{ name: "a", value: 100, extra: "new" }]);
    });
    it("should not update non-matching elements", () => {
      const doc = {
        _id: "1",
        _class: "test",
        arr: [
          { name: "a", value: 1 },
          { name: "b", value: 2 }
        ]
      };
      const operator = (0, import_operator._getOperator)("$update");
      operator(doc, {
        arr: {
          $query: { name: "c" },
          $update: { value: 100 }
        }
      });
      expect(doc.arr).toEqual([
        { name: "a", value: 1 },
        { name: "b", value: 2 }
      ]);
    });
  });
  describe("$inc operator", () => {
    it("should increment existing number", () => {
      const doc = { _id: "1", _class: "test", count: 5 };
      const operator = (0, import_operator._getOperator)("$inc");
      operator(doc, { count: 3 });
      expect(doc.count).toBe(8);
    });
    it("should initialize undefined field to increment value", () => {
      const doc = { _id: "1", _class: "test" };
      const operator = (0, import_operator._getOperator)("$inc");
      operator(doc, { count: 10 });
      expect(doc.count).toBe(10);
    });
    it("should handle negative increment", () => {
      const doc = { _id: "1", _class: "test", count: 10 };
      const operator = (0, import_operator._getOperator)("$inc");
      operator(doc, { count: -3 });
      expect(doc.count).toBe(7);
    });
    it("should increment multiple fields", () => {
      const doc = { _id: "1", _class: "test", a: 1, b: 2 };
      const operator = (0, import_operator._getOperator)("$inc");
      operator(doc, { a: 10, b: 20 });
      expect(doc.a).toBe(11);
      expect(doc.b).toBe(22);
    });
    it("should handle zero increment", () => {
      const doc = { _id: "1", _class: "test", count: 5 };
      const operator = (0, import_operator._getOperator)("$inc");
      operator(doc, { count: 0 });
      expect(doc.count).toBe(5);
    });
  });
  describe("$unset operator", () => {
    it("should remove existing field", () => {
      const doc = { _id: "1", _class: "test", field: "value" };
      const operator = (0, import_operator._getOperator)("$unset");
      operator(doc, { field: "" });
      expect(doc.field).toBeUndefined();
    });
    it("should handle undefined field", () => {
      const doc = { _id: "1", _class: "test" };
      const operator = (0, import_operator._getOperator)("$unset");
      operator(doc, { field: "" });
      expect(doc.field).toBeUndefined();
    });
    it("should unset multiple fields", () => {
      const doc = { _id: "1", _class: "test", a: 1, b: 2, c: 3 };
      const operator = (0, import_operator._getOperator)("$unset");
      operator(doc, { a: "", c: "" });
      expect(doc.a).toBeUndefined();
      expect(doc.b).toBe(2);
      expect(doc.c).toBeUndefined();
    });
    it("should unset nested objects", () => {
      const doc = { _id: "1", _class: "test", obj: { nested: "value" } };
      const operator = (0, import_operator._getOperator)("$unset");
      operator(doc, { obj: "" });
      expect(doc.obj).toBeUndefined();
    });
  });
  describe("$rename operator", () => {
    it("should rename existing field", () => {
      const doc = { _id: "1", _class: "test", oldName: "value" };
      const operator = (0, import_operator._getOperator)("$rename");
      operator(doc, { oldName: "newName" });
      expect(doc.oldName).toBeUndefined();
      expect(doc.newName).toBe("value");
    });
    it("should handle undefined field", () => {
      const doc = { _id: "1", _class: "test" };
      const operator = (0, import_operator._getOperator)("$rename");
      operator(doc, { oldName: "newName" });
      expect(doc.oldName).toBeUndefined();
      expect(doc.newName).toBeUndefined();
    });
    it("should rename multiple fields", () => {
      const doc = { _id: "1", _class: "test", a: 1, b: 2 };
      const operator = (0, import_operator._getOperator)("$rename");
      operator(doc, { a: "x", b: "y" });
      expect(doc.a).toBeUndefined();
      expect(doc.b).toBeUndefined();
      expect(doc.x).toBe(1);
      expect(doc.y).toBe(2);
    });
    it("should overwrite existing field with same name", () => {
      const doc = {
        _id: "1",
        _class: "test",
        old: "oldValue",
        new: "existingValue"
      };
      const operator = (0, import_operator._getOperator)("$rename");
      operator(doc, { old: "new" });
      expect(doc.old).toBeUndefined();
      expect(doc.new).toBe("oldValue");
    });
    it("should preserve object references when renaming", () => {
      const obj = { nested: "value" };
      const doc = { _id: "1", _class: "test", oldName: obj };
      const operator = (0, import_operator._getOperator)("$rename");
      operator(doc, { oldName: "newName" });
      expect(doc.newName).toBe(obj);
    });
  });
  describe("_getOperator", () => {
    it("should return correct operator functions", () => {
      expect((0, import_operator._getOperator)("$push")).toBeDefined();
      expect((0, import_operator._getOperator)("$pull")).toBeDefined();
      expect((0, import_operator._getOperator)("$update")).toBeDefined();
      expect((0, import_operator._getOperator)("$inc")).toBeDefined();
      expect((0, import_operator._getOperator)("$unset")).toBeDefined();
      expect((0, import_operator._getOperator)("$rename")).toBeDefined();
    });
    it("should throw error for unknown operator", () => {
      expect(() => (0, import_operator._getOperator)("$unknown")).toThrow("unknown operator: $unknown");
      expect(() => (0, import_operator._getOperator)("$invalid")).toThrow("unknown operator: $invalid");
    });
  });
  describe("operator error handling", () => {
    describe("$pull with non-array values", () => {
      it("should handle string value instead of array", () => {
        const doc = { _id: "1", _class: "test", field: "not-an-array" };
        const operator = (0, import_operator._getOperator)("$pull");
        operator(doc, { field: "value" });
        expect(doc.field).toEqual([]);
      });
      it("should handle object value instead of array", () => {
        const doc = { _id: "1", _class: "test", field: { key: "value" } };
        const operator = (0, import_operator._getOperator)("$pull");
        operator(doc, { field: "value" });
        expect(doc.field).toEqual([]);
      });
      it("should handle number value instead of array", () => {
        const doc = { _id: "1", _class: "test", field: 42 };
        const operator = (0, import_operator._getOperator)("$pull");
        operator(doc, { field: "value" });
        expect(doc.field).toEqual([]);
      });
      it("should handle null value", () => {
        const doc = { _id: "1", _class: "test", field: null };
        const operator = (0, import_operator._getOperator)("$pull");
        operator(doc, { field: "value" });
        expect(doc.field).toEqual([]);
      });
      it("should work correctly with valid array", () => {
        const doc = { _id: "1", _class: "test", field: [1, 2, 3] };
        const operator = (0, import_operator._getOperator)("$pull");
        operator(doc, { field: 2 });
        expect(doc.field).toEqual([1, 3]);
      });
    });
    describe("$update with non-array values", () => {
      it("should handle string value instead of array", () => {
        const doc = { _id: "1", _class: "test", field: "not-an-array" };
        const operator = (0, import_operator._getOperator)("$update");
        operator(doc, { field: { $query: { id: 1 }, $update: { value: "new" } } });
        expect(doc.field).toEqual([]);
      });
      it("should handle object value instead of array", () => {
        const doc = { _id: "1", _class: "test", field: { key: "value" } };
        const operator = (0, import_operator._getOperator)("$update");
        operator(doc, { field: { $query: { id: 1 }, $update: { value: "new" } } });
        expect(doc.field).toEqual([]);
      });
      it("should handle null value", () => {
        const doc = { _id: "1", _class: "test", field: null };
        const operator = (0, import_operator._getOperator)("$update");
        operator(doc, { field: { $query: { id: 1 }, $update: { value: "new" } } });
        expect(doc.field).toEqual([]);
      });
      it("should work correctly with valid array", () => {
        const doc = {
          _id: "1",
          _class: "test",
          field: [
            { id: 1, value: "old" },
            { id: 2, value: "keep" }
          ]
        };
        const operator = (0, import_operator._getOperator)("$update");
        operator(doc, { field: { $query: { id: 1 }, $update: { value: "new" } } });
        expect(doc.field).toEqual([
          { id: 1, value: "new" },
          { id: 2, value: "keep" }
        ]);
      });
    });
    describe("$inc with non-numeric values", () => {
      it("should handle NaN current value", () => {
        const doc = { _id: "1", _class: "test", count: NaN };
        const operator = (0, import_operator._getOperator)("$inc");
        operator(doc, { count: 5 });
        expect(doc.count).toBe(5);
      });
      it("should handle string current value", () => {
        const doc = { _id: "1", _class: "test", count: "not-a-number" };
        const operator = (0, import_operator._getOperator)("$inc");
        operator(doc, { count: 5 });
        expect(doc.count).toBe(5);
      });
      it("should handle NaN increment value", () => {
        const doc = { _id: "1", _class: "test", count: 10 };
        const operator = (0, import_operator._getOperator)("$inc");
        operator(doc, { count: NaN });
        expect(doc.count).toBe(10);
      });
      it("should handle string increment value", () => {
        const doc = { _id: "1", _class: "test", count: 10 };
        const operator = (0, import_operator._getOperator)("$inc");
        operator(doc, { count: "not-a-number" });
        expect(doc.count).toBe(10);
      });
      it("should handle undefined current value (should default to 0)", () => {
        const doc = { _id: "1", _class: "test" };
        const operator = (0, import_operator._getOperator)("$inc");
        operator(doc, { count: 5 });
        expect(doc.count).toBe(5);
      });
      it("should work correctly with valid numbers", () => {
        const doc = { _id: "1", _class: "test", count: 10 };
        const operator = (0, import_operator._getOperator)("$inc");
        operator(doc, { count: 5 });
        expect(doc.count).toBe(15);
      });
      it("should handle negative increments", () => {
        const doc = { _id: "1", _class: "test", count: 10 };
        const operator = (0, import_operator._getOperator)("$inc");
        operator(doc, { count: -3 });
        expect(doc.count).toBe(7);
      });
      it("should handle zero increment", () => {
        const doc = { _id: "1", _class: "test", count: 10 };
        const operator = (0, import_operator._getOperator)("$inc");
        operator(doc, { count: 0 });
        expect(doc.count).toBe(10);
      });
    });
  });
});
//# sourceMappingURL=operator.test.js.map
